"""ai/prompts/website_intelligence_prompt.py — Mission 11.

"The exact instructions we give the AI": the prompt text that tells the model to
return only JSON matching the WebsiteIntelligence schema, with sourced fields.
"""

from agent3.ai import schemas
