# Agent 3 — Website Intelligence Extraction Agent
# Core dependencies referenced by the module wiring (see Agent3_Architecture.md).
# Pin exact versions once each mission's implementation lands.

# Data contracts / validation (ai/schemas.py, api/schemas.py)
pydantic

# Crawler (fetcher.py, link_discovery.py)
requests
beautifulsoup4

# API layer (api/routes.py)
fastapi
uvicorn

# AI / LLM extraction (ai/extractor.py — Mission 11)
anthropic

# RAG add-on (ai/rag/*) — OPTIONAL. Mission 11's fixed pipeline runs without
# these; they're only needed if you enable the semantic-retrieval layer
# (Agent3_Architecture.md §11). The extractor selects chunks by page-type
# priority when RAG isn't wired up, so it has no hard numpy/faiss dependency.
numpy
faiss-cpu

# Tests
pytest
